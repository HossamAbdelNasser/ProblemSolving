class Solution {
public:
    int findFinalValue(vector<int>& nums, int original) {

        set<int> st;

        for (int n : nums) {
            st.insert(n);
        }

        while (st.find(original) != st.end()) {
                original *= 2;
        }

        return original;
    }
};
