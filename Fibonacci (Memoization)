#include <iostream>

using namespace std;

int memo[10];

int fib(int n){
    
    int f;
    
    if (memo[n]) return memo[n];
    
    if (n <= 2) return 1;
    
    memo[n] = fib(n-1) + fib(n-2);
    
    return memo[n];
}

int main() {
    cout << fib(10) << endl;

    return 0;
}
